pipeline {
    agent any

    stages {
 
        stage("Run tests") {
            steps {
                echo "####testing####"
                bat "pytest test_add.py"
            }
        }
    }
}
